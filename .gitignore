# Virtual environments
.venv/
venv/

# Python
__pycache__/
*.py[cod]

# Testing & coverage
.pytest_cache/
.coverage
htmlcov/

# Linting & type-checking caches
.ruff_cache/
.mypy_cache/

# Environment variables
.env

# Build artifacts
dist/
build/
*.egg-info/

# OpenWiki generates an AGENTS.md this project doesn't use; keep it untracked
# so `openwiki code --update` (locally or in CI) never accidentally commits it.
AGENTS.md
